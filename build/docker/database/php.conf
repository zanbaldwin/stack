[mysqld]
default_time_zone='+00:00'
# Use the old MySQL 5.6 version of authentication by uncommenting the next line if you are using PHP <7.4 (which was
# when support for the MySQL 8+'s `caching_sha2_password` authentication plugin was introduced).
#default_authentication_plugin='mysql_native_password'
